Compile and run in Eclipse.

To spawn demo units, press Q or W.
To select units, drag using left mouse button.
To command selected units to move, use the right mouse button.
Navigate the map by left clicking the minimap on the bottom right.

At the top left of the map, there are some red walls. Command the units to move around there to see the pathing engine in action.